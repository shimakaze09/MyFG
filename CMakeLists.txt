CMAKE_MINIMUM_REQUIRED(VERSION 3.20 FATAL_ERROR)

PROJECT(MyFG VERSION 0.2.0 LANGUAGES CXX)
MESSAGE(STATUS "Project: ${PROJECT_NAME} ${PROJECT_VERSION}")

INCLUDE(cmake/InitMyCMake.cmake)
INIT_MYCMAKE()

INIT_PROJECT()

ADD_DEP(MyGraphviz 0.1.4)

SET(MyFG_BUILD_DX12 TRUE CACHE BOOL "Build Frame Graph for DirectX 12")

IF (MyFG_BUILD_DX12)
    ADD_DEP(MyDX12 0.0.5)
ENDIF ()

ADD_SUB_DIRS_REC(src)

EXPORT_TARGETS(DIRECTORIES "include")